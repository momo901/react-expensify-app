import React from 'react';
import ReactDOM from 'react-dom';

const AboutPage=()=>(
    <div>
        <h2>about page </h2>
        <p> i'm new react app devlopper, so be patience  </p>
    </div>
);

export default AboutPage;